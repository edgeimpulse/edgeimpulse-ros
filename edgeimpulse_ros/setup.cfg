[metadata]
name = edgeimpulse_ros
version = 0.1.0
description = ROS2 wrapper to run Edge Impulse object detection

[options]
packages = find:
install_requires =
    rclpy
    std_msgs

[options.entry_points]
console_scripts =
    edgeimpulse_detector = edgeimpulse_ros.edgeimpulse_detector:main
